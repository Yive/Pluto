--- a/net/minecraft/world/entity/item/ItemEntity.java
+++ b/net/minecraft/world/entity/item/ItemEntity.java
@@ -128,6 +_,7 @@
     @Override
     public void inactiveTick() {
         super.inactiveTick();
+        if (foundHopper()) return; // Pluto - Entities search for hoppers
         if (this.pickupDelay > 0 && this.pickupDelay != 32767) {
             this.pickupDelay--;
         }
@@ -147,12 +_,19 @@
     }
     // Paper end - EAR 2
 
+    // Pluto start - Entities search for hoppers
+    public boolean acceptItem(net.minecraft.world.level.block.entity.HopperBlockEntity hopper) {
+        return net.minecraft.world.level.block.entity.HopperBlockEntity.addItem(hopper, this);
+    }
+    // Pluto end - Entities search for hoppers
+
     @Override
     public void tick() {
         if (this.getItem().isEmpty()) {
             this.discard(org.bukkit.event.entity.EntityRemoveEvent.Cause.DESPAWN); // CraftBukkit - add Bukkit remove cause
         } else {
             super.tick();
+            if (foundHopper()) return; // Pluto - Entities search for hoppers
             if (this.pickupDelay > 0 && this.pickupDelay != 32767) {
                 this.pickupDelay--;
             }
@@ -189,7 +_,7 @@
                     friction = 1F;
                 } else if (this.onGround()) {
                     // Paper end - Friction API
-                    friction = this.level().getBlockState(this.getBlockPosBelowThatAffectsMyMovement()).getBlock().getFriction() * 0.98F;
+                    friction = this.level().pluto$getBlockState(this.pluto$getBlockPosBelowThatAffectsMyMovement()).getBlock().getFriction() * 0.98F; // Pluto - Memory allocation optimisations
                 }
 
                 this.setDeltaMovement(this.getDeltaMovement().multiply(friction, 0.98, friction));
@@ -201,11 +_,11 @@
                 }
             }
 
-            boolean moved = Mth.floor(this.xo) != Mth.floor(this.getX())
-                || Mth.floor(this.yo) != Mth.floor(this.getY())
-                || Mth.floor(this.zo) != Mth.floor(this.getZ());
-            int rate = moved ? 2 : 40;
-            if (this.tickCount % rate == 0 && !this.level().isClientSide() && this.isMergable()) {
+            // Pluto start - Configuration for item merging
+            boolean flag = this.xo != this.getX() || this.yo != this.getY() || this.zo != this.getZ(); // Flooring makes the movement check inconsistent
+            int i = flag ? this.level().plutoConfig().itemMergeMovingInterval : this.level().plutoConfig().itemMergeStaticInterval;
+            if ((!flag || !this.level().plutoConfig().disableItemMergeCheckWhileMoving) && this.tickCount % i == 0 && !this.level().isClientSide() && this.isMergable()) {
+            // Pluto end - Configuration for item merging
                 this.mergeWithNeighbours();
             }
 
@@ -238,6 +_,13 @@
         return this.getOnPos(0.999999F);
     }
 
+    // Pluto start - Memory allocation optimisations
+    @Override
+    public long pluto$getBlockPosBelowThatAffectsMyMovement() {
+        return this.pluto$getOnPos((float) DELTA_AFFECTED_BY_BLOCKS_BELOW_1_0);
+    }
+    // Pluto end - Memory allocation optimisations
+
     private void setUnderwaterMovement() {
         this.setFluidMovement(0.99F);
     }
@@ -254,8 +_,11 @@
     private void mergeWithNeighbours() {
         if (this.isMergable()) {
             double radius = this.level().spigotConfig.itemMerge; // Spigot
-            for (ItemEntity entity : this.level()
-                .getEntitiesOfClass(ItemEntity.class, this.getBoundingBox().inflate(radius, this.level().paperConfig().entities.behavior.onlyMergeItemsHorizontally ? 0 : radius - 0.5D, radius), other -> other != this && other.isMergable())) { // Spigot // Paper - configuration to only merge items horizontally
+            // Pluto start - Implement limit for item's merge with neighbours action
+            java.util.List<ItemEntity> entities = new java.util.ArrayList<>();
+            this.level().moonrise$getEntityLookup().getEntities(ItemEntity.class, null, this.getBoundingBox().inflate(radius, this.level().paperConfig().entities.behavior.onlyMergeItemsHorizontally ? 0 : radius - 0.5D, radius), entities, other -> other != this && other.isMergable(), this.level().plutoConfig().itemMergeWithNeighboursLimit);
+            for (ItemEntity entity : entities) { // Spigot // Paper - configuration to only merge items horizontally
+                // Pluto end - Implement limit for item's merge with neighbours action
                 if (entity.isMergable()) {
                     // Paper start - Fix items merging through walls
                     if (this.level().paperConfig().fixes.fixItemsMergingThroughWalls) {
