--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -456,7 +_,7 @@
         profiler.push("livingEntityBaseTick");
         if (this.isAlive() && this.level() instanceof ServerLevel level) {
             boolean isPlayer = this instanceof Player;
-            if (this.isInWall()) {
+            if (shouldCheckForSuffocation() && this.isInWall()) { // Pufferfish - optimize suffocation
                 this.hurtServer(level, this.damageSources().inWall(), 1.0F);
             } else if (isPlayer && !level.getWorldBorder().isWithinBounds(this.getBoundingBox())) {
                 double dist = level.getWorldBorder().getDistanceToBorder(this) + level.getWorldBorder().getSafeZone();
@@ -469,7 +_,7 @@
             }
 
             if (this.isEyeInFluid(FluidTags.WATER)
-                && !level.getBlockState(BlockPos.containing(this.getX(), this.getEyeY(), this.getZ())).is(Blocks.BUBBLE_COLUMN)) {
+                && !level.pluto$getBlockState(Mth.floor(this.getX()), Mth.floor(this.getEyeY()), Mth.floor(this.getZ())).is(Blocks.BUBBLE_COLUMN)) { // Pluto - Memory allocation optimisations
                 boolean canDrownInWater = !this.canBreatheUnderwater()
                     && !MobEffectUtil.hasWaterBreathing(this)
                     && (!isPlayer || !((Player)this).getAbilities().invulnerable);
@@ -3084,10 +_,10 @@
     }
 
     private void travelInAir(final Vec3 input) {
-        BlockPos posBelow = this.getBlockPosBelowThatAffectsMyMovement();
+        long blockPosBelowThatAffectsMyMovement = this.pluto$getBlockPosBelowThatAffectsMyMovement(); // Pluto - Memory allocation optimisations
         float blockFriction = this.onGround()
             ? computeModifiedFriction(
-                this.level().getBlockState(posBelow).getBlock().getFriction(), (float)this.getAttributeValue(Attributes.FRICTION_MODIFIER)
+                this.level().pluto$getBlockState(blockPosBelowThatAffectsMyMovement).getBlock().getFriction(), (float)this.getAttributeValue(Attributes.FRICTION_MODIFIER) // Pluto - Memory allocation optimisations
             )
             : 1.0F;
         Vec3 movement = this.handleRelativeFrictionAndCalculateMovement(input, blockFriction);
@@ -3095,7 +_,7 @@
         MobEffectInstance levitationEffect = this.getEffect(MobEffects.LEVITATION);
         if (levitationEffect != null) {
             movementY += (0.05 * (levitationEffect.getAmplifier() + 1) - movement.y) * 0.2;
-        } else if (!this.level().isClientSide() || this.level().hasChunkAt(posBelow)) {
+        } else if (!this.level().isClientSide() || this.level().hasChunkAt(BlockPos.getX(blockPosBelowThatAffectsMyMovement) >> 4, BlockPos.getZ(blockPosBelowThatAffectsMyMovement) >> 4)) { // Pluto - Memory allocation optimisations
             movementY -= this.getEffectiveGravity();
         } else if (this.getY() > this.level().getMinY()) {
             movementY = -0.1;
@@ -3807,6 +_,7 @@
             this.checkAutoSpinAttack(beforeTravelBox, this.getBoundingBox());
         }
 
+        if (this.level().plutoConfig().entityPushingModulo > 0 && (this.level().plutoConfig().entityPushingModulo == 1 || this.tickCount % this.level().plutoConfig().entityPushingModulo == 0)) // Pluto - Configuration for entity pushing frequency
         this.pushEntities();
         profiler.pop();
         // Paper start - Add EntityMoveEvent
@@ -3897,6 +_,7 @@
             return;
         }
         // Paper end - don't run getEntities if we're not going to use its result
+        if (this.level().plutoConfig().onlyPlayersPushEntities && !(this instanceof ServerPlayer)) return; // Pluto - Option to make only players push entities
         List<Entity> pushableEntities = this.level().getPushableEntities(this, this.getBoundingBox());
         if (!pushableEntities.isEmpty()) {
             if (this.level() instanceof ServerLevel serverLevel) {
@@ -4532,10 +_,13 @@
         WorldBorder worldBorder = level.getWorldBorder();
         Vec3 clamped = worldBorder.clampVec3ToBound(xx, y, zz);
         BlockPos.MutableBlockPos pos = BlockPos.containing(clamped).mutable();
-        if (level.hasChunkAt(pos)) {
+        // Pluto start - Decrease chunk/block lookups
+        net.minecraft.world.level.chunk.LevelChunk chunk = level.getChunkIfLoaded(pos.getX() >> 4, pos.getZ() >> 4);
+        if (chunk != null) {
             while (pos.getY() > level.getMinY()) {
                 pos.move(Direction.DOWN);
-                BlockState state = level.getBlockState(pos);
+                BlockState state = chunk.getBlockState(pos);
+                // Pluto end - Decrease chunk/block lookups
                 if (state.is(BlockTags.ENTITIES_CAN_TELEPORT_TO)) {
                     return this.checkPositionAndTeleport(clamped.x, y, clamped.z, showParticles, isInvalidPosition, state, level, cause); // Paper - teleport cause API
                 }
@@ -4725,8 +_,7 @@
                 this.level().setBlockAndUpdate(bedPosition, state.setValue(BedBlock.OCCUPIED, false));
                 bedBlock.onStopSleeping(this.level(), bedPosition);
                 Vec3 standUp = AbstractBedBlock.findStandUpPosition(this.getType(), this.level(), bedPosition, facing, this.getYRot()).orElseGet(() -> {
-                    BlockPos above = bedPosition.above();
-                    return new Vec3(above.getX() + 0.5, above.getY() + 0.1, above.getZ() + 0.5);
+                    return new Vec3(bedPosition.getX() + 0.5, bedPosition.getY() + 1.1, bedPosition.getZ() + 0.5); // Pluto - Memory allocation optimisations
                 });
                 Vec3 lookDirection = Vec3.atBottomCenterOf(bedPosition).subtract(standUp).normalize();
                 float yaw = (float)Mth.wrapDegrees(Mth.atan2(lookDirection.z, lookDirection.x) * 180.0F / (float)Math.PI - 90.0);
@@ -4750,6 +_,19 @@
     public boolean isInWall() {
         return !this.isSleeping() && super.isInWall();
     }
+
+    // Pufferfish start - optimize suffocation
+    public boolean couldPossiblyBeHurt(float amount) {
+        if ((float) getInvulnerableTime() > (float) this.invulnerableDuration / 2.0F && amount <= this.lastHurt) {
+            return false;
+        }
+        return true;
+    }
+
+    public boolean shouldCheckForSuffocation() {
+        return !gg.pufferfish.pufferfish.PufferfishConfig.enableSuffocationOptimization || (tickCount % 10 == 0 && couldPossiblyBeHurt(1.0F));
+    }
+    // Pufferfish end
 
     public ItemStack getProjectile(final ItemStack heldWeapon) {
         return ItemStack.EMPTY;
