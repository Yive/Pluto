--- a/net/minecraft/world/entity/projectile/ThrownEnderpearl.java
+++ b/net/minecraft/world/entity/projectile/ThrownEnderpearl.java
@@ -193,11 +_,13 @@
         }
 
         if (this.isAlive()) {
-            BlockPos blockPos = BlockPos.containing(this.position());
+            long blockPos = this.position().asBlockPosLong(); // Pluto - Memory allocation optimisations
             if ((
                     --this.ticketTimer <= 0L
-                        || sectionPosX != SectionPos.blockToSectionCoord(blockPos.getX())
-                        || sectionPosZ != SectionPos.blockToSectionCoord(blockPos.getZ())
+                        // Pluto start - Memory allocation optimisations
+                        || sectionPosX != SectionPos.blockToSectionCoord(BlockPos.getX(blockPos))
+                        || sectionPosZ != SectionPos.blockToSectionCoord(BlockPos.getZ(blockPos))
+                        // Pluto end - Memory allocation optimisations
                 )
                 && owner instanceof ServerPlayer serverPlayer1) {
                 this.ticketTimer = serverPlayer1.registerAndUpdateEnderPearlTicket(this);
