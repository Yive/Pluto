--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -387,6 +_,8 @@
     public long activatedTick = Integer.MIN_VALUE;
     public boolean isTemporarilyActive;
     public long activatedImmunityTick = Integer.MIN_VALUE;
+    public boolean activatedPriorityReset = false; // Pufferfish - DAB
+    public int activatedPriority = gg.pufferfish.pufferfish.PufferfishConfig.maximumActivationPrio; // Pufferfish - DAB (golf score)
 
     public void inactiveTick() {
     }
@@ -857,6 +_,14 @@
             return;
         }
         // Paper end - entity despawn time limit
+
+        // Pluto start - TTL setting for entities from spawners
+        if (type != EntityType.PLAYER && spawnedViaMobSpawner && type.spawnerTTL >= 0 && this.tickCount >= type.spawnerTTL) {
+            this.discard(org.bukkit.event.entity.EntityRemoveEvent.Cause.DESPAWN);
+            return;
+        }
+        // Pluto end - TTL setting for entities from spawners
+
         this.baseTick();
     }
 
@@ -884,7 +_,7 @@
         }
 
         if (this instanceof ServerPlayer) this.handlePortal(); // CraftBukkit - Moved up to postTick
-        if (this.canSpawnSprintParticle()) {
+        if (!this.level.plutoConfig().disableSprintParticles && this.canSpawnSprintParticle()) { // Pluto - Option to disable sprint particles | More efficient to exit early to avoid canSpawnSprintParticle
             this.spawnSprintParticle();
         }
 
@@ -1215,10 +_,13 @@
                 this.minorHorizontalCollision = false;
             }
 
-            BlockPos effectPos = this.getOnPosLegacy();
-            BlockState effectState = this.level().getBlockState(effectPos);
+            // Pluto start - Memory allocation optimisations
+            long effectPosLong = this.pluto$getOnPosLegacy();
+            BlockPos effectPos = null;
+            BlockState effectState = this.level().pluto$getBlockState(effectPosLong);
             if (this.isLocalInstanceAuthoritative()) {
-                this.checkFallDamage(movement.y, this.onGround(), effectState, effectPos);
+                this.checkFallDamage(movement.y, this.onGround(), effectState, effectPos = BlockPos.of(effectPosLong));
+                // Pluto end - Memory allocation optimisations
             }
 
             if (this.isRemoved()) {
@@ -1260,7 +_,7 @@
                 if (!this.level().isClientSide() || this.isLocalInstanceAuthoritative()) {
                     Entity.MovementEmission emission = this.getMovementEmission();
                     if (emission.emitsAnything() && !this.isPassenger()) {
-                        this.applyMovementEmissionAndPlaySound(emission, movement, effectPos, effectState);
+                        this.applyMovementEmissionAndPlaySound(emission, movement, effectPos == null ? BlockPos.of(effectPosLong) : effectPos, effectState); // Pluto - Memory allocation optimisations
                     }
                 }
 
@@ -1284,12 +_,16 @@
         float moveDistScale = 0.6F;
         float movedDistance = (float)(clippedMovement.length() * 0.6F);
         float horizontalMovedDistance = (float)(clippedMovement.horizontalDistance() * 0.6F);
-        BlockPos supportingPos = this.getOnPos();
-        BlockState supportingState = this.level().getBlockState(supportingPos);
+        // Pluto start - Memory allocation optimisations
+        long onPosLong = this.pluto$getOnPos();
+        BlockPos supportingPos;
+        BlockState supportingState = this.level().pluto$getBlockState(onPosLong);
+        // Pluto end - Memory allocation optimisations
         boolean climbing = this.isStateClimbable(supportingState);
         this.moveDist += climbing ? movedDistance : horizontalMovedDistance;
         this.flyDist += movedDistance;
         if (this.moveDist > this.nextStep && !supportingState.isAir()) {
+            supportingPos = BlockPos.of(onPosLong); // Pluto - Memory allocation optimisations | Moved down
             boolean onlyEffectStateEmittions = supportingPos.equals(effectPos);
             boolean producedSideEffects = this.vibrationAndSoundEffectsFromBlock(
                 effectPos, effectState, emission.emitsSounds(), onlyEffectStateEmittions, clippedMovement
@@ -1372,6 +_,7 @@
             boolean wasOnFire = this.isOnFire();
             boolean wasFreezing = this.isFreezing();
             int previousRemainingFireTicks = this.getRemainingFireTicks();
+            if (this.level().plutoConfig().entityInsideBlockCheck > 0 && (this.level().plutoConfig().entityInsideBlockCheck == 1 || this.tickCount % this.level().plutoConfig().entityInsideBlockCheck == 0)) // Pluto - Configuration for entity inside block check frequency
             this.checkInsideBlocks(movements, this.insideEffectCollector);
             this.insideEffectCollector.applyAndClear(this);
             if (this.isInRain()) {
@@ -1451,7 +_,7 @@
     protected void processFlappingMovement() {
         if (this.isFlapping()) {
             this.onFlap();
-            if (this.getMovementEmission().emitsEvents()) {
+            if (!this.level().plutoConfig().disableFlapEvents && this.getMovementEmission().emitsEvents()) { // Pluto - Option to disable flapping movement game event
                 this.gameEvent(GameEvent.FLAP);
             }
         }
@@ -1471,13 +_,13 @@
     }
 
     protected BlockPos getOnPos(final float offset) {
-        if (this.mainSupportingBlockPos.isPresent() && this.level().getChunkIfLoadedImmediately(this.mainSupportingBlockPos.get()) != null) { // Paper - ensure no loads
+        if (this.mainSupportingBlockPos.isPresent() && this.level().getChunkIfLoadedImmediately(this.mainSupportingBlockPos.get()) instanceof net.minecraft.world.level.chunk.ChunkAccess chunk) { // Paper - ensure no loads // Pluto - Decrease chunk/block lookups
             BlockPos getOnPos = this.mainSupportingBlockPos.get();
             if (!(offset > 1.0E-5F)) {
                 return getOnPos;
             }
 
-            BlockState belowState = this.level().getBlockState(getOnPos);
+            BlockState belowState = chunk.getBlockState(getOnPos); // Pluto - Decrease chunk/block lookups
             return (!(offset <= 0.5) || !belowState.is(BlockTags.FENCES))
                     && !belowState.is(BlockTags.WALLS)
                     && !(belowState.getBlock() instanceof FenceGateBlock)
@@ -1491,9 +_,45 @@
         }
     }
 
+    // Pluto start - Memory allocation optimisations
+    @Deprecated
+    public long pluto$getOnPosLegacy() {
+        return this.pluto$getOnPos(DELTA_AFFECTED_BY_BLOCKS_BELOW_0_2);
+    }
+
+    public long pluto$getOnPos() {
+        return this.pluto$getOnPos(Mth.EPSILON);
+    }
+
+    public long pluto$getBlockPosBelowThatAffectsMyMovement() {
+        return this.pluto$getOnPos((float) DELTA_AFFECTED_BY_BLOCKS_BELOW_0_5);
+    }
+
+    protected long pluto$getOnPos(final float offset) {
+        if (this.mainSupportingBlockPos.isPresent() && this.level().getChunkIfLoadedImmediately(this.mainSupportingBlockPos.get()) instanceof net.minecraft.world.level.chunk.ChunkAccess chunk) { // Paper - ensure no loads // Pluto - Decrease chunk/block lookups
+            BlockPos getOnPos = this.mainSupportingBlockPos.get();
+            if (!(offset > 1.0E-5F)) {
+                return getOnPos.asLong();
+            } else {
+                BlockState belowState = chunk.getBlockState(getOnPos); // Pluto - Decrease chunk/block lookups
+                return (!(offset <= 0.5) || !belowState.is(BlockTags.FENCES))
+                    && !belowState.is(BlockTags.WALLS)
+                    && !(belowState.getBlock() instanceof FenceGateBlock)
+                    ? BlockPos.asLong(getOnPos.getX(), Mth.floor(this.position.y - offset), getOnPos.getZ())
+                    : getOnPos.asLong();
+            }
+        } else {
+            int xTruncated = Mth.floor(this.position.x);
+            int yTruncatedBelow = Mth.floor(this.position.y - offset);
+            int zTruncated = Mth.floor(this.position.z);
+            return BlockPos.asLong(xTruncated, yTruncatedBelow, zTruncated);
+        }
+    }
+    // Pluto end - Memory allocation optimisations
+
     protected float getBlockJumpFactor() {
         float jumpFactorHere = this.level().getBlockState(this.blockPosition()).getBlock().getJumpFactor();
-        float jumpFactorBelow = this.level().getBlockState(this.getBlockPosBelowThatAffectsMyMovement()).getBlock().getJumpFactor();
+        float jumpFactorBelow = this.level().pluto$getBlockState(this.pluto$getBlockPosBelowThatAffectsMyMovement()).getBlock().getJumpFactor(); // Pluto - Memory allocation optimisations
         return jumpFactorHere == 1.0 ? jumpFactorBelow : jumpFactorHere;
     }
 
@@ -1502,7 +_,7 @@
         float speedFactorHere = state.getBlock().getSpeedFactor();
         if (!state.is(Blocks.WATER) && !state.is(Blocks.BUBBLE_COLUMN)) {
             return speedFactorHere == 1.0
-                ? this.level().getBlockState(this.getBlockPosBelowThatAffectsMyMovement()).getBlock().getSpeedFactor()
+                ? this.level().pluto$getBlockState(this.pluto$getBlockPosBelowThatAffectsMyMovement()).getBlock().getSpeedFactor() // Pluto - Memory allocation optimisations
                 : speedFactorHere;
         } else {
             return speedFactorHere;
@@ -1879,9 +_,10 @@
     }
 
     protected BlockPos getPrimaryStepSoundBlockPos(final BlockPos affectingPos) {
-        BlockPos abovePos = affectingPos.above();
-        BlockState aboveState = this.level().getBlockState(abovePos);
-        return !aboveState.is(BlockTags.INSIDE_STEP_SOUND_BLOCKS) && !aboveState.is(BlockTags.COMBINATION_STEP_SOUND_BLOCKS) ? affectingPos : abovePos;
+        // Pluto start - Memory allocation optimisations
+        BlockState blockState = this.level().pluto$getBlockState(affectingPos.getX(), affectingPos.getY() + 1, affectingPos.getZ());
+        return !blockState.is(BlockTags.INSIDE_STEP_SOUND_BLOCKS) && !blockState.is(BlockTags.COMBINATION_STEP_SOUND_BLOCKS) ? affectingPos : affectingPos.above();
+        // Pluto end - Memory allocation optimisations
     }
 
     protected void playCombinationStepSounds(final BlockState primaryStepSound, final BlockState secondaryStepSound) {
@@ -2031,9 +_,24 @@
         return this.wasTouchingWater;
     }
 
+    private int lastRainCheck = -1; private boolean lastRainCheckResult = false; // Pluto - Optimise rain check
     public boolean isInRain() {
+        // Pluto start - Optimise rain check
+        if (this.tickCount - this.lastRainCheck < this.level().plutoConfig().entityRainCheckRate) {
+            return this.lastRainCheckResult;
+        }
+
+        this.lastRainCheck = this.tickCount;
+
+        // Dimensions without skylight can't have rain.
+        if (!this.level().dimensionType().hasSkyLight()) {
+            this.lastRainCheckResult = false;
+            return false;
+        }
         BlockPos pos = this.blockPosition();
-        return this.level().isRainingAt(pos) || this.level().isRainingAt(BlockPos.containing(pos.getX(), this.getBoundingBox().maxY, pos.getZ()));
+        this.lastRainCheckResult = this.level().isRainingAt(pos) || this.level().isRainingAt(BlockPos.containing(pos.getX(), this.getBoundingBox().maxY, pos.getZ()));
+        return this.lastRainCheckResult;
+        // Pluto end - Optimise rain check
     }
 
     public boolean isInWaterOrRain() {
@@ -2137,11 +_,11 @@
 
     @Deprecated
     protected BlockState getBlockStateOnLegacy() {
-        return this.level().getBlockState(this.getOnPosLegacy());
+        return this.level().pluto$getBlockState(this.pluto$getOnPosLegacy()); // Pluto - Memory allocation optimisations
     }
 
     public BlockState getBlockStateOn() {
-        return this.level().getBlockState(this.getOnPos());
+        return this.level().pluto$getBlockState(this.pluto$getOnPos()); // Pluto - Memory allocation optimisations
     }
 
     public boolean canSpawnSprintParticle() {
@@ -2149,19 +_,25 @@
     }
 
     protected void spawnSprintParticle() {
-        BlockPos pos = this.getOnPosLegacy();
-        BlockState blockState = this.level().getBlockState(pos);
+        // Pluto start - Memory allocation optimisations
+        long posLong = this.pluto$getOnPosLegacy();
+        BlockState blockState = this.level().pluto$getBlockState(posLong);
+        // Pluto end - Memory allocation optimisations
         if (blockState.getRenderShape() != RenderShape.INVISIBLE) {
             Vec3 movement = this.getDeltaMovement();
             BlockPos entityPosition = this.blockPosition();
             double x = this.getX() + (this.random.nextDouble() - 0.5) * this.dimensions.width();
             double z = this.getZ() + (this.random.nextDouble() - 0.5) * this.dimensions.width();
-            if (entityPosition.getX() != pos.getX()) {
-                x = Mth.clamp(x, pos.getX(), pos.getX() + 1.0);
+            // Pluto start - Memory allocation optimisations
+            final int x1 = BlockPos.getX(posLong);
+            if (entityPosition.getX() != x1) {
+                x = Mth.clamp(x, x1, x1 + 1.0);
             }
 
-            if (entityPosition.getZ() != pos.getZ()) {
-                z = Mth.clamp(z, pos.getZ(), pos.getZ() + 1.0);
+            final int z1 = BlockPos.getZ(posLong);
+            if (entityPosition.getZ() != z1) {
+                z = Mth.clamp(z, z1, z1 + 1.0);
+                // Pluto end - Memory allocation optimisations
             }
 
             this.level()
@@ -5375,6 +_,42 @@
             return this.save;
         }
     }
+
+    // Pluto start - Add method to obtain current entity id
+    public static int currentEntityId() {
+        return ENTITY_COUNTER.get();
+    }
+    // Pluto end - Add method to obtain current entity id
+
+    // Pluto start - Entities search for hoppers
+    public net.minecraft.world.level.block.entity.@Nullable HopperBlockEntity findHopper() {
+        int originX = this.getBlockX();
+        int originY = this.getBlockY();
+        int originZ = this.getBlockZ();
+
+        BlockPos.MutableBlockPos adjacentPos = new BlockPos.MutableBlockPos();
+        net.minecraft.world.level.chunk.LevelChunk chunk = this.level().getChunkIfLoaded(originX >> 4, originZ >> 4);
+        if (chunk == null) return null;
+
+        for (int y = originY; y > originY - 2; --y) {
+            adjacentPos.set(originX, y, originZ);
+            net.minecraft.world.level.block.entity.HopperBlockEntity hopper = chunk.getBlockEntity(adjacentPos) instanceof net.minecraft.world.level.block.entity.HopperBlockEntity hopperBlockEntity ? hopperBlockEntity : null;
+            if (hopper == null) continue;
+            return hopper;
+        }
+        return null;
+    }
+
+    public boolean foundHopper() {
+        if (!this.level().plutoConfig().entitiesSearchForHoppers || tickCount % this.level().plutoConfig().ticksBetweenEntitiesSearchForHoppers != 0) return false;
+        net.minecraft.world.level.block.entity.HopperBlockEntity hopper = findHopper();
+        return hopper != null && hopper.canAcceptItems() && acceptItem(hopper);
+    }
+
+    public boolean acceptItem(net.minecraft.world.level.block.entity.HopperBlockEntity hopper) {
+        return false;
+    }
+    // Pluto end - Entities search for hoppers
 
     // Paper start - Expose entity id counter
     public static int nextEntityId() {
