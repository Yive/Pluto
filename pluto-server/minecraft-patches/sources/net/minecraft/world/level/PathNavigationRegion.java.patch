--- a/net/minecraft/world/level/PathNavigationRegion.java
+++ b/net/minecraft/world/level/PathNavigationRegion.java
@@ -123,6 +_,56 @@
         return chunk.getBlockEntity(pos);
     }
 
+    // Pluto start - Memory allocation optimisations
+    @Override
+    public BlockState pluto$getBlockState(final int x, final int y, final int z) {
+        if (this.isOutsideBuildHeight(y))
+            return Blocks.AIR.defaultBlockState();
+
+        return this.getChunk(x >> 4, z >> 4).getBlockState(x, y, z);
+    }
+
+    @Override
+    public BlockState pluto$getBlockState(final long packedPos) {
+        return pluto$getBlockState(BlockPos.getX(packedPos), BlockPos.getY(packedPos), BlockPos.getZ(packedPos));
+    }
+
+    @Override
+    public @Nullable BlockState pluto$getBlockStateIfLoaded(final int x, final int y, final int z) {
+        ChunkAccess chunk = getChunkIfLoaded(x >> 4, z >> 4);
+        return chunk == null ? null : chunk.getBlockState(x, y, z);
+    }
+
+    @Override
+    public @Nullable BlockState pluto$getBlockStateIfLoaded(long packedPos) {
+        return pluto$getBlockStateIfLoaded(BlockPos.getX(packedPos), BlockPos.getY(packedPos), BlockPos.getZ(packedPos));
+    }
+
+    @Override
+    public FluidState pluto$getFluidState(final int x, final int y, final int z) {
+        if (this.isOutsideBuildHeight(y))
+            return Fluids.EMPTY.defaultFluidState();
+
+        return this.getChunk(x >> 4, z >> 4).pluto$getFluidState(x, y, z);
+    }
+
+    @Override
+    public FluidState pluto$getFluidState(final long packedPos) {
+        return pluto$getFluidState(BlockPos.getX(packedPos), BlockPos.getY(packedPos), BlockPos.getZ(packedPos));
+    }
+
+    @Override
+    public @Nullable FluidState pluto$getFluidStateIfLoaded(final int x, final int y, final int z) {
+        ChunkAccess chunk = getChunkIfLoaded(x >> 4, z >> 4);
+        return chunk == null ? null : chunk.pluto$getFluidState(x, y, z);
+    }
+
+    @Override
+    public @Nullable FluidState pluto$getFluidStateIfLoaded(final long packedPos) {
+        return pluto$getFluidStateIfLoaded(BlockPos.getX(packedPos), BlockPos.getY(packedPos), BlockPos.getZ(packedPos));
+    }
+    // Pluto end - Memory allocation optimisations
+
     @Override
     public BlockState getBlockState(final BlockPos pos) {
         if (this.isOutsideBuildHeight(pos)) {
