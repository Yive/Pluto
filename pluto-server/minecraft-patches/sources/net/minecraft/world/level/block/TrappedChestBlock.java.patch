--- a/net/minecraft/world/level/block/TrappedChestBlock.java
+++ b/net/minecraft/world/level/block/TrappedChestBlock.java
@@ -53,4 +_,16 @@
     protected int getDirectSignal(final BlockState state, final BlockGetter level, final BlockPos pos, final Direction direction) {
         return direction == Direction.UP ? state.getSignal(level, pos, direction) : Redstone.SIGNAL_MIN;
     }
+
+    // Pluto start - Memory allocation optimisations
+    @Override
+    protected int pluto$getSignal(BlockState state, BlockGetter blockAccess, int x, int y, int z, Direction direction) {
+        return Mth.clamp(ChestBlockEntity.pluto$getOpenCount(blockAccess, x, y, z), 0, 15);
+    }
+
+    @Override
+    protected int pluto$getDirectSignal(BlockState blockState, BlockGetter blockAccess, int x, int y, int z, Direction side) {
+        return side == Direction.UP ? blockState.pluto$getSignal(blockAccess, x, y, z, side) : Redstone.SIGNAL_MIN;
+    }
+    // Pluto end - Memory allocation optimisations
 }
