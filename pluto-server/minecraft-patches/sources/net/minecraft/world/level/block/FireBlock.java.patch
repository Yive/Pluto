--- a/net/minecraft/world/level/block/FireBlock.java
+++ b/net/minecraft/world/level/block/FireBlock.java
@@ -131,15 +_,19 @@
     }
 
     protected BlockState getStateForPlacement(final BlockGetter level, final BlockPos pos) {
-        BlockPos below = pos.below();
-        BlockState belowState = level.getBlockState(below);
-        if (!this.canBurn(belowState) && !belowState.isFaceSturdy(level, below, Direction.UP)) {
+        // Pluto start - Memory allocation optimisations
+        final int x = pos.getX();
+        final int y = pos.getY();
+        final int z = pos.getZ();
+        BlockState belowState = level.pluto$getBlockState(x, y - 1, z);
+        if (!this.canBurn(belowState) && !belowState.isFaceSturdy(level, pos.below(), Direction.UP)) {
+            // Pluto end - Memory allocation optimisations
             BlockState result = this.defaultBlockState();
 
-            for (Direction direction : Direction.values()) {
+            for (Direction direction : Direction.VALUES) { // Pluto - Avoid excessive calls to Enum#values
                 BooleanProperty property = PROPERTY_BY_DIRECTION.get(direction);
                 if (property != null) {
-                    result = result.setValue(property, this.canBurn(level.getBlockState(pos.relative(direction))));
+                    result = result.setValue(property, this.canBurn(level.pluto$getBlockState(x + direction.getStepX(), y + direction.getStepY(), z + direction.getStepZ()))); // Pluto - Memory allocation optimisations
                 }
             }
 
@@ -306,8 +_,13 @@
     }
 
     private boolean isValidFireLocation(final BlockGetter level, final BlockPos pos) {
-        for (Direction direction : Direction.values()) {
-            if (this.canBurn(level.getBlockState(pos.relative(direction)))) {
+        // Pluto start - Memory allocation optimisations
+        final int x = pos.getX();
+        final int y = pos.getY();
+        final int z = pos.getZ();
+        for (Direction direction : Direction.VALUES) { // Pluto - Avoid excessive calls to Enum#values
+            if (this.canBurn(level.pluto$getBlockState(x + direction.getStepX(), y + direction.getStepY(), z + direction.getStepZ()))) {
+                // Pluto end - Memory allocation optimisations
                 return true;
             }
         }
@@ -321,8 +_,13 @@
         } else {
             int odds = 0;
 
-            for (Direction direction : Direction.values()) {
-                BlockState blockState = level.getBlockState(pos.relative(direction));
+            // Pluto start - Memory allocation optimisations
+            final int x = pos.getX();
+            final int y = pos.getY();
+            final int z = pos.getZ();
+            for (Direction direction : Direction.VALUES) { // Pluto - Avoid excessive calls to Enum#values
+                BlockState blockState = level.pluto$getBlockState(x + direction.getStepX(), y + direction.getStepY(), z + direction.getStepZ());
+                // Pluto end - Memory allocation optimisations
                 odds = Math.max(this.getIgniteOdds(blockState), odds);
             }
 
