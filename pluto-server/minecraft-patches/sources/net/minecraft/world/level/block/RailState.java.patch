--- a/net/minecraft/world/level/block/RailState.java
+++ b/net/minecraft/world/level/block/RailState.java
@@ -102,14 +_,17 @@
         if (BaseRailBlock.isRail(testState)) {
             return new RailState(this.level, pos, testState);
         } else {
-            BlockPos testPos = pos.above();
-            testState = this.level.getBlockState(testPos);
+            // Pluto start - Memory allocation optimisations
+            final int x = pos.getX();
+            final int y = pos.getY();
+            final int z = pos.getZ();
+            testState = this.level.pluto$getBlockState(x, y + 1, z);
             if (BaseRailBlock.isRail(testState)) {
-                return new RailState(this.level, testPos, testState);
+                return new RailState(this.level, pos.above(), testState);
             } else {
-                testPos = pos.below();
-                testState = this.level.getBlockState(testPos);
-                return BaseRailBlock.isRail(testState) ? new RailState(this.level, testPos, testState) : null;
+                testState = this.level.pluto$getBlockState(x, y - 1, z);
+                return BaseRailBlock.isRail(testState) ? new RailState(this.level, pos.below(), testState) : null;
+                // Pluto end - Memory allocation optimisations
             }
         }
     }
@@ -132,7 +_,7 @@
     protected int countPotentialConnections() {
         int count = 0;
 
-        for (Direction direction : Direction.Plane.HORIZONTAL) {
+        for (Direction direction : Direction.Plane.HORIZONTAL.faces) { // Pluto - Expose Direction$Plane's faces
             if (this.hasRail(this.pos.relative(direction))) {
                 count++;
             }
