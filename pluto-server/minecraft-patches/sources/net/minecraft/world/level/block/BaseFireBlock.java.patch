--- a/net/minecraft/world/level/block/BaseFireBlock.java
+++ b/net/minecraft/world/level/block/BaseFireBlock.java
@@ -43,8 +_,7 @@
     }
 
     public static BlockState getState(final BlockGetter level, final BlockPos pos) {
-        BlockPos below = pos.below();
-        BlockState belowState = level.getBlockState(below);
+        BlockState belowState = level.pluto$getBlockState(pos.getX(), pos.getY() - 1, pos.getZ()); // Pluto - Memory allocation optimisations
         return SoulFireBlock.canSurviveOnBlock(belowState) ? Blocks.SOUL_FIRE.defaultBlockState() : ((FireBlock)Blocks.FIRE).getStateForPlacement(level, pos);
     }
 
@@ -171,7 +_,7 @@
     @Override
     protected void onPlace(final BlockState state, final Level level, final BlockPos pos, final BlockState oldState, final boolean movedByPiston, net.minecraft.world.item.context.UseOnContext context) { // CraftBukkit - context
         if (!oldState.is(state.getBlock())) {
-            if (inPortalDimension(level)) {
+            if (!level.plutoConfig().disableNetherPortalGeneration && inPortalDimension(level)) { // Pluto - Toggle for nether portal generation
                 Optional<PortalShape> optionalShape = PortalShape.findEmptyPortalShape(level, pos, Direction.Axis.X);
                 if (optionalShape.isPresent()) {
                     optionalShape.get().createPortalBlocks(level, (context == null) ? null : context.getPlayer()); // CraftBukkit - player
@@ -211,11 +_,15 @@
         if (!inPortalDimension(level)) {
             return false;
         } else {
-            BlockPos.MutableBlockPos testPos = pos.mutable();
+            // Pluto start - Memory allocation optimisations
+            final int x = pos.getX();
+            final int y = pos.getY();
+            final int z = pos.getZ();
+            // Pluto end - Memory allocation optimisations
             boolean hasObsidian = false;
 
-            for (Direction face : Direction.values()) {
-                if (level.getBlockState(testPos.set(pos).move(face)).is(Blocks.OBSIDIAN)) {
+            for (Direction face : Direction.VALUES) { // Pluto - Avoid excessive calls to Enum#values
+                if (level.pluto$getBlockState(x + face.getStepX(), y + face.getStepY(), z + face.getStepZ()).is(Blocks.OBSIDIAN)) { // Pluto - Memory allocation optimisations
                     hasObsidian = true;
                     break;
                 }
