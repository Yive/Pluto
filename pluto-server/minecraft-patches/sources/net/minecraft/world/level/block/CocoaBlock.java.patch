--- a/net/minecraft/world/level/block/CocoaBlock.java
+++ b/net/minecraft/world/level/block/CocoaBlock.java
@@ -60,7 +_,10 @@
 
     @Override
     protected boolean canSurvive(final BlockState state, final LevelReader level, final BlockPos pos) {
-        BlockState relativeState = level.getBlockState(pos.relative(state.getValue(FACING)));
+        // Pluto start - Memory allocation optimisations
+        Direction direction = state.getValue(FACING);
+        BlockState relativeState = level.pluto$getBlockState(pos.getX() + direction.getStepX(), pos.getY() + direction.getStepY(), pos.getZ() + direction.getStepZ());
+        // Pluto end - Memory allocation optimisations
         return relativeState.is(BlockTags.SUPPORTS_COCOA);
     }
 
