--- a/net/minecraft/world/level/block/DetectorRailBlock.java
+++ b/net/minecraft/world/level/block/DetectorRailBlock.java
@@ -86,6 +_,22 @@
         }
     }
 
+    // Pluto start - Memory allocation optimisations
+    @Override
+    protected int pluto$getSignal(final BlockState state, final BlockGetter level, final int x, final int y, final int z, final Direction direction) {
+        return state.getValue(POWERED) ? Redstone.SIGNAL_MAX : Redstone.SIGNAL_MIN;
+    }
+
+    @Override
+    protected int pluto$getDirectSignal(final BlockState state, final BlockGetter level, final int x, final int y, final int z, final Direction direction) {
+        if (!state.getValue(POWERED)) {
+            return Redstone.SIGNAL_MIN;
+        } else {
+            return direction == Direction.UP ? Redstone.SIGNAL_MAX : Redstone.SIGNAL_MIN;
+        }
+    }
+    // Pluto end - Memory allocation optimisations
+
     private void checkPressed(final Level level, final BlockPos pos, final BlockState state) {
         if (this.canSurvive(state, level, pos)) {
             if (!state.is(this)) { return; } // Paper - Fix some rails connecting improperly
