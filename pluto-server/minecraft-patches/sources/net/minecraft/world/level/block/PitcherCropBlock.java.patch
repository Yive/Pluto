--- a/net/minecraft/world/level/block/PitcherCropBlock.java
+++ b/net/minecraft/world/level/block/PitcherCropBlock.java
@@ -139,7 +_,7 @@
 
     @Override
     public void randomTick(BlockState state, ServerLevel level, BlockPos pos, RandomSource random) {
-        float growthSpeed = CropBlock.getGrowthSpeed(this, level, pos);
+        float growthSpeed = level.plutoConfig().useStaticGrowthSpeedPitchers ? level.plutoConfig().staticGrowthSpeedPitchers : CropBlock.getGrowthSpeed(this, level, pos); // Pluto - Static growth speeds
         boolean flag = random.nextFloat() < (level.spigotConfig.pitcherPlantModifier / (100.0F * (Math.floor(25.0F / growthSpeed) + 1))); // Paper - Fix Spigot growth modifiers
         if (flag) {
             this.grow(level, state, pos, 1);
@@ -163,7 +_,7 @@
     }
 
     private static boolean sufficientLight(LevelReader level, BlockPos pos) {
-        return CropBlock.hasSufficientLight(level, pos);
+        return (level instanceof ServerLevel serverLevel && serverLevel.plutoConfig().pitcherCropIgnoreLightLevel) || CropBlock.hasSufficientLight(level, pos); // Pluto - Toggle for light lookups on certain flora
     }
 
     private static boolean isLower(BlockState state) {
@@ -186,9 +_,10 @@
         if (isLower(state)) {
             return new PitcherCropBlock.PosAndState(pos, state);
         } else {
-            BlockPos blockPos = pos.below();
-            BlockState blockState = level.getBlockState(blockPos);
-            return isLower(blockState) ? new PitcherCropBlock.PosAndState(blockPos, blockState) : null;
+            // Pluto start - Memory allocation optimisations
+            BlockState blockState = level.pluto$getBlockState(pos.getX(), pos.getY() - 1, pos.getZ());
+            return isLower(blockState) ? new PitcherCropBlock.PosAndState(pos.below(), blockState) : null;
+            // Pluto end - Memory allocation optimisations
         }
     }
 
