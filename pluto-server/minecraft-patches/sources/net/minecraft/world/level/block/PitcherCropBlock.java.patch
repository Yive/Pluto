--- a/net/minecraft/world/level/block/PitcherCropBlock.java
+++ b/net/minecraft/world/level/block/PitcherCropBlock.java
@@ -147,7 +_,7 @@
 
     @Override
     public void randomTick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
-        float growthSpeed = CropBlock.getGrowthSpeed(this, level, pos);
+        float growthSpeed = level.plutoConfig().useStaticGrowthSpeedPitchers ? level.plutoConfig().staticGrowthSpeedPitchers : CropBlock.getGrowthSpeed(this, level, pos); // Pluto - Static growth speeds
         boolean shouldProgressGrowth = random.nextFloat() < (level.spigotConfig.pitcherPlantModifier / (100.0F * (Math.floor(25.0F / growthSpeed) + 1))); // Paper - Fix Spigot growth modifiers
         if (shouldProgressGrowth) {
             this.grow(level, state, pos, 1);
@@ -171,7 +_,7 @@
     }
 
     private static boolean sufficientLight(final LevelReader level, final BlockPos pos) {
-        return CropBlock.hasSufficientLight(level, pos);
+        return (level instanceof ServerLevel serverLevel && serverLevel.plutoConfig().pitcherCropIgnoreLightLevel) || CropBlock.hasSufficientLight(level, pos); // Pluto - Toggle for light lookups on certain flora
     }
 
     private static boolean isLower(final BlockState state) {
@@ -197,9 +_,10 @@
         if (isLower(state)) {
             return new PitcherCropBlock.PosAndState(pos, state);
         } else {
-            BlockPos lowerPos = pos.below();
-            BlockState lowerState = level.getBlockState(lowerPos);
-            return isLower(lowerState) ? new PitcherCropBlock.PosAndState(lowerPos, lowerState) : null;
+            // Pluto start - Memory allocation optimisations
+            BlockState lowerState = level.pluto$getBlockState(pos.getX(), pos.getY() - 1, pos.getZ());
+            return isLower(lowerState) ? new PitcherCropBlock.PosAndState(pos.below(), lowerState) : null;
+            // Pluto end - Memory allocation optimisations
         }
     }
 
