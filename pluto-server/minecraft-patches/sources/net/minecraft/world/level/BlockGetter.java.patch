--- a/net/minecraft/world/level/BlockGetter.java
+++ b/net/minecraft/world/level/BlockGetter.java
@@ -31,6 +_,20 @@
         return blockEntity != null && blockEntity.getType() == blockEntityType ? Optional.of((T)blockEntity) : Optional.empty();
     }
 
+    // Pluto start - Memory efficient block lookups
+    BlockState pluto$getBlockState(int x, int y, int z);
+    BlockState pluto$getBlockState(long packedPos);
+
+    @Nullable BlockState pluto$getBlockStateIfLoaded(int x, int y, int z);
+    @Nullable BlockState pluto$getBlockStateIfLoaded(long packedPos);
+
+    FluidState pluto$getFluidState(int x, int y, int z);
+    FluidState pluto$getFluidState(long packedPos);
+
+    @Nullable FluidState pluto$getFluidStateIfLoaded(int x, int y, int z);
+    @Nullable FluidState pluto$getFluidStateIfLoaded(long packedPos);
+    // Pluto end - Memory efficient block lookups
+
     BlockState getBlockState(BlockPos pos);
 
     // Paper start - if loaded util
