--- a/net/minecraft/world/SimpleContainer.java
+++ b/net/minecraft/world/SimpleContainer.java
@@ -60,6 +_,13 @@
         return this.bukkitOwner;
     }
 
+    // Pluto start - Allow toggling snapshots for certain blocks
+    @Override
+    public org.bukkit.inventory.@org.jspecify.annotations.Nullable InventoryHolder getOwner(boolean useSnapshot) {
+        return getOwner();
+    }
+    // Pluto end - Allow toggling snapshots for certain blocks
+
     @Override
     public org.bukkit.@org.jspecify.annotations.Nullable Location getLocation() {
         // Paper start - Fix inventories returning null Locations
@@ -221,6 +_,8 @@
     public boolean stillValid(final Player player) {
         return true;
     }
+
+    @Override public boolean stillValid() { return true; } // Pluto - Hoppers cache containers
 
     @Override
     public void clearContent() {
